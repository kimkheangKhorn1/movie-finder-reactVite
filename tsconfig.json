{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": "src", // Ensure this is set
    "paths": {
      "*": ["*"] // Ensure this allows flexible imports
    }
  }
}
